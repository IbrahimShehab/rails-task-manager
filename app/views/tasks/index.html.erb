<h1>My Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>
    <% if task.completed? %>
      ✅  <!-- Checkmark for completed task -->
    <% else %>
      ⬜  <!-- Empty checkbox for incomplete task -->
    <% end %>
      <%= link_to task.title, task_path(task) %>
      .
      <%= link_to "🗑", task_path(task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, title: "Delete", class: "text-danger" %>

    </li>
  <% end %>
</ul>

<%= link_to "Add a new task", tasks_new_path, class: "btn btn-primary btn-sm" %>
